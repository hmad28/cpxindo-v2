import type { Metadata } from "next";import Link from "next/link";import { redirect } from "next/navigation";import { AuthForm } from "@/components/auth-form";import { login } from "@/lib/actions/auth";import { getSession } from "@/lib/auth/session";
export const metadata:Metadata={title:"Sign In"};
export default async function LoginPage({searchParams}:{searchParams:Promise<{next?:string}>}){const session=await getSession();const {next}=await searchParams;if(session)redirect(session.role==="admin"?"/admin":"/account");return <main className="auth-page"><Link className="logo" href="/"><span className="logo-mark"/><span>CPX<small>JERSEY</small></span></Link><section className="auth-card"><span className="kicker">WELCOME BACK</span><h1>SIGN IN.</h1><p>Akses pesanan, alamat, dan benefit member CPX kamu.</p><AuthForm mode="login" action={login} next={next}/></section></main>}
