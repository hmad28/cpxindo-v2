import type { Metadata } from "next";import { Header } from "@/components/header";import { Footer } from "@/components/footer";import { ProductCard } from "@/components/product-card";import { getProducts } from "@/lib/products";
export const metadata:Metadata={title:"Products",description:"Shop jersey, training wear, dan sportswear CPX."};
export default async function ProductsPage({searchParams}:{searchParams:Promise<{category?:string}>}){const {category}=await searchParams;const products=await getProducts({category});const categories=["All","Football","Basketball","Training"];return <><Header/><main className="page-shell"><div className="page-hero"><span className="kicker">CPX COLLECTION</span><h1>BUILT TO<br/><em>PERFORM.</em></h1><p>Sportswear yang bergerak bersama kamu—di lapangan dan di luar pertandingan.</p></div><div className="catalog-toolbar"><div>{categories.map(c=><a className={(!category&&c==="All")||category===c?"active":""} href={c==="All"?"/products":`/products?category=${c}`} key={c}>{c}</a>)}</div><span>{products.length} PRODUCTS</span></div><div className="product-grid catalog-grid">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div></main><Footer/></>}
