import { count, eq, sql } from "drizzle-orm";import Link from "next/link";import { BarChart3, Package, ShoppingBag, Users } from "@/components/icons";import { db, isDatabaseConfigured } from "@/lib/db";import { orders, products, users } from "@/lib/db/schema";import { formatIDR } from "@/lib/products";
export default async function AdminPage(){let productCount=4,orderCount=0,userCount=2,revenue=0;if(db){const [p,o,u,r]=await Promise.all([db.select({value:count()}).from(products).where(eq(products.active,true)),db.select({value:count()}).from(orders),db.select({value:count()}).from(users),db.select({value:sql<number>`coalesce(sum(${orders.total}),0)`}).from(orders).where(eq(orders.status,"completed"))]);productCount=p[0].value;orderCount=o[0].value;userCount=u[0].value;revenue=Number(r[0].value)}return <><div className="admin-heading"><div><span>DASHBOARD</span><h1>Store overview</h1></div><Link href="/admin/products" className="admin-primary">+ Add product</Link></div>{!isDatabaseConfigured&&<div className="demo-alert"><b>Demo mode</b><span>Connect `DATABASE_URL` and run migrations to enable persistent CRUD.</span></div>}<div className="stat-grid"><article><Package/><span>Active products</span><b>{productCount}</b></article><article><ShoppingBag/><span>Total orders</span><b>{orderCount}</b></article><article><Users/><span>Customers</span><b>{userCount}</b></article><article><BarChart3/><span>Completed revenue</span><b>{formatIDR(revenue)}</b></article></div><section className="admin-panel"><div className="panel-head"><div><h2>Quick actions</h2><p>Manage the essentials of your CPX store.</p></div></div><div className="quick-actions"><Link href="/admin/products"><Package/><b>Manage products</b><span>Stock, price, and catalog visibility</span></Link><Link href="/admin/orders"><ShoppingBag/><b>Process orders</b><span>Update payment and fulfillment status</span></Link></div></section></>}
