PORT=3000
NODE_ENV=development

# Neon PostgreSQL
DATABASE_URL=

# Admin auth — generate with: node -e "const bcrypt = require('bcryptjs'); console.log(bcrypt.hashSync('YOUR_PASSCODE', 10))"
ADMIN_PASSCODE_HASH=

# Session secret for cookie signing
SESSION_SECRET=
